{
  "name": "gecko_reporting_workflow",
  "version": "0.1.0",
  "private": true,
  "description": "Slack to Jira workflow bridge for Gecko reporting flows.",
  "type": "module",
  "engines": {
    "node": ">=20.11.0"
  },
  "scripts": {
    "dev": "node --import tsx src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@slack/bolt": "^4.2.1",
    "@slack/types": "^2.18.0",
    "dotenv": "^16.4.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^24.0.10",
    "tsx": "^4.19.1",
    "typescript": "^5.8.3"
  }
}
